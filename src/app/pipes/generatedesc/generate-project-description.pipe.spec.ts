import { GenerateProjectDescriptionPipe } from './generate-project-description.pipe';

describe('GeneratedescPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateProjectDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
